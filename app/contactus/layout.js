export const metadata = {
  title: 'Contact US',
  description: 'Gontact Us page description',
};

export default function ContactUslayout({ children = null }) {
  return <section>{children}</section>;
}
